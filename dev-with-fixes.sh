#!/bin/bash

# This script calls the dev-with-replit-fixes.sh script
# It serves as a compatibility layer for the existing .replit configuration

echo "Starting development with fixes..."
./dev-with-replit-fixes.sh