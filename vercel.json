{
  "version": 2,
  "framework": null,
  "buildCommand": "npm install --no-save @aws-sdk/client-s3 @aws-sdk/s3-request-presigner @types/node @types/express && npm run check && node vercel-custom-build.js",
  "installCommand": "echo 'Using custom buildCommand instead'",
  "outputDirectory": "public",
  "devCommand": "npm run dev",
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        { "key": "Cache-Control", "value": "s-maxage=0" }
      ]
    }
  ],
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" },
    { "source": "/(.*)", "destination": "/" }
  ]
}